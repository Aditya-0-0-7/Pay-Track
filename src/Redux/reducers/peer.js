export default (state={},action)=>{
    switch (action.type)
    {
        case 'PEER_':
            {
                
            }
        default:
            {
                return state;
            }
    }
}